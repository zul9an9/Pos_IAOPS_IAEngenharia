# fumaca

**Prompt:** o pod do nyx-prod não sobe

**Lab:** lab-com-skills

**Skills:** ['metacortex-triagem']

**Ferramentas:** 5 chamadas

**Tentativas de escrita:** nenhuma

---

Log vazio confirma: a aplicação não teve tempo de reportar erro — a morte veio do kernel (OOM), não de uma falha própria do app.

```
# Triagem — nyx-prod — "o pod não sobe"

**Causa:** container — `nyx-api` é morto por OOM (limite de memória insuficiente: `limits.memory: 24Mi`)
**Evidência:**
- describe pod nyx-api-7665bb6885-6z4hd: Last State Terminated, Reason: OOMKilled, Exit Code: 137, Restart Count: 34
- kubectl_logs --previous: log vazio — confirma que a morte não veio de um erro reportado pela aplicação, e sim de um kill externo (OOM)
**Funcionando ao lado:** nyx-postgres-5f5f4d5847-pd4hw está 1/1 Running, 0 restarts — banco descartado como causa
**Hipóteses descartadas:** falha na aplicação/DB — logs vazios e postgres saudável descartam isso; problema de imagem/registry — Image ID presente e Pulled normal, não é ImagePullBackOff
**Correção sugerida (NÃO aplicada):** aumentar `resources.limits.memory` do container `api` no Deployment `nyx-api` (24Mi é muito baixo para uma app Node/kube-news) — ajuste de manifesto passa pela skill `metacortex-manifests` e por PR com revisão
**Caminho percorrido:**
- kubectl_get pods -n nyx-prod -o wide → nyx-api em CrashLoopBackOff (2 réplicas), nyx-postgres saudável
- kubectl_describe pod nyx-api-7665bb6885-6z4hd → Last State OOMKilled, Exit Code 137, limit memory 24Mi
- kubectl_logs --previous no mesmo pod → vazio, confirma morte externa (OOM)
```
