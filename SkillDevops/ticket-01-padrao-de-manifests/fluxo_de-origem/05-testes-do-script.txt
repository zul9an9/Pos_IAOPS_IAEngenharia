$ conferir.py teste-do-script/   # 3.3 em ConfigMap e comentário, 1.1, 1.3 managed-by
## Falhas — barram a subida

| Regra | Recurso | Detalhe | Fonte |
|---|---|---|---|
| 1.1 | `ConfigMap/helio_config` | 'helio_config' não é kebab-case minúsculo (sem camelCase, underscore ou ponto) | script |
| 1.3 | `ConfigMap/helio_config` | managed-by='kubectl' — valores válidos: platform | argocd | helm | script |
| 3.3 | `ConfigMap/helio_config` | chave 'DB_PASSWORD' com cara de segredo em ConfigMap — usar Secret | script |
| 3.3 | `configmap-com-segredo.yaml:9` | URL com usuário:senha em texto (comentário) | script |

## Avisos — recomendado; exceção precisa de justificativa no PR

$ conferir.py --sem-trivy manifests/   # degradação sem Trivy
Trivy: NÃO executado (desativado por --sem-trivy)
| 2.1 | obrigatório | requests e limits de CPU e memória | não verificado (sem trivy) | trivy |
| 3.1 | proibido | Tag :latest | **falha (1)** | trivy + script |
| 3.2 | obrigatório | securityContext (non-root, UID 10001, sem escalonamento, FS read-only, drop ALL) | não verificado (sem trivy) | trivy |
| 3.6 | proibido | hostNetwork, hostPID, privileged | não verificado (sem trivy) | trivy |
| 3.1 | `Deployment/NyxAPI:api` | tag 'latest' — :latest/sem tag é proibido | script |
**Total:** 12 falha(s), 2 aviso(s), 6 ponto(s) a revisar lendo o projeto.

$ conferir.py /nao/existe
ERRO ao ler manifests: /nao/existe
código de saída: 2
